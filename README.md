# HighlighsJZ — API Livescore Multi-langage

API Livescore en temps réel avec **Python** (collecteur), **TypeScript** (API REST + WebSocket), **Go** (cache haute perf), **Rust** (client WebSocket).

## Architecture
